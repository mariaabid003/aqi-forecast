# -------- Core dependencies --------
requests
pandas>=2.0,<3.0
numpy
scikit-learn
matplotlib
tensorflow
shap
streamlit>=1.30.0
flask
python-dotenv

# -------- Hopsworks Feature Store --------
# Must match backend version (4.2.2) and requires Python >=3.11
hopsworks==4.2.2
hsfs==3.8.0

# -------- Google Cloud AI Platform --------
google-cloud-aiplatform

# -------- Optional: workflow management --------
# Excluded during CI/CD because it’s heavy
apache-airflow
